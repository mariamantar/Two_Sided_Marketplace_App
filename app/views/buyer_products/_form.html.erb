<%= form_with(model: buyer_product, local: true) do |form| %>
  <% if buyer_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buyer_product.errors.count, "error") %> prohibited this buyer_product from being saved:</h2>

      <ul>
      <% buyer_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Buyer_id %>
    <%= form.text_field :Buyer_id %>
  </div>

  <div class="field">
    <%= form.label :Product_id %>
    <%= form.text_field :Product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
